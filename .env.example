# GitHub Authentication
# DEBUG=false
# Set to 'true' to enable verbose logging
# Debug Mode (Optional)

# GIT_COMMITTER_EMAIL=your.email@example.com
# GIT_COMMITTER_NAME=Your Name
# GIT_AUTHOR_EMAIL=your.email@example.com
# GIT_AUTHOR_NAME=Your Name
# Git Configuration (Optional)

# DEFAULT_BASE=main
# Default base branch for PRs

# DEFAULT_REPO=pr-mirror
# Default repository name

# DEFAULT_ORG=PandasWhoCode
# Default GitHub organization

# Uncomment and set values as needed
# You can set default values here to avoid passing flags every time
# Default Configuration (Optional)

# GITHUB_UNAME=
# GITHUB_TOKEN=
# You don't need to set these manually - they're populated by the script
# These are automatically retrieved from GitHub CLI (gh) during runtime

